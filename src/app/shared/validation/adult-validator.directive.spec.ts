import { AdultValidatorDirective } from './adult-validator.directive';

describe('AdultValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AdultValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
