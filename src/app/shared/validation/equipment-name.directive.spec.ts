import { EquipmentNameDirective } from './equipment-name.directive';

describe('EquipmentNameDirective', () => {
  it('should create an instance', () => {
    const directive = new EquipmentNameDirective();
    expect(directive).toBeTruthy();
  });
});
