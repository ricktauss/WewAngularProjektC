import {  } from './adult-validator.directive';
import { EndDateValidatorDirective } from "./enddate-validator.directive";

describe('EndDateValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EndDateValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
