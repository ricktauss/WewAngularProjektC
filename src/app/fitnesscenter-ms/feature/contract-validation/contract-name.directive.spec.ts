import { ContractNameValidatorDirective } from './contract-name.directive';

describe('ContractNameValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ContractNameValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
